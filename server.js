'use strict';
const http=require('http'),fs=require('fs'),path=require('path'),crypto=require('crypto'),DATA=require('./shared-game-data');
const PORT=Number(process.env.PORT)||8080,TICK_RATE=25,MAX_PLAYERS=4,GRACE_MS=15000,LOBBY_TTL=30*60e3,GAME_TTL=2*60*60e3,alphabet='ABCDEFGHJKLMNPQRSTUVWXYZ23456789';
const allowedMessages=new Set(['hello','createLobby','joinLobby','selectMap','selectCharacter','playerReady','startGame','playerInput','upgradeChoice','returnToLobby']);
const limits={hello:500,createLobby:1500,joinLobby:700,selectMap:250,selectCharacter:250,playerReady:200,startGame:500,playerInput:25,upgradeChoice:250,returnToLobby:500};
const MAP_TERRAIN={ruins:[{type:'wall',x:840,y:360,w:390,h:42},{type:'wall',x:1190,y:360,w:42,h:290},{type:'rock',x:1530,y:470,r:77},{type:'rock',x:2450,y:520,r:97}],toxic:[{type:'wall',x:620,y:430,w:480,h:38},{type:'wall',x:2030,y:620,w:38,h:460},{type:'rock',x:1480,y:620,r:88},{type:'rock',x:2290,y:1510,r:101}]};
const finite=(n,min,max)=>Number.isFinite(n)&&n>=min&&n<=max,short=(v,n)=>typeof v==='string'&&v.length>0&&v.length<=n;
function blocked(map,x,y,r){if(!finite(x,0,3000)||!finite(y,0,2200)||x<35+r||y<35+r||x>2965-r||y>2165-r)return true;return(MAP_TERRAIN[map]||[]).some(t=>t.type==='rock'?Math.hypot(x-t.x,y-t.y)<r+t.r:x+r>t.x&&x-r<t.x+t.w&&y+r>t.y&&y-r<t.y+t.h)}
function newStats(character){const base=DATA.characters[character];return{damage:base.damage,fireRate:base.fireRate,speed:base.speed,maxHp:base.hp,projectileSpeed:base.projectileSpeed,projectileSize:4,projectileLife:1.3,projectiles:1,pierce:0,critChance:.1,critDamage:1.8,lifesteal:0,thorns:0,cooldowns:{q:0,e:0,r:0}}}
function applyUpgrade(player,key){const [kind,id]=String(key).split('-');if(kind==='augment'){if(!DATA.augments.includes(id)||player.augments.includes(id)||player.augments.length>=3)return false;player.augments.push(id);if(id==='glass_cannon'){player.stats.damage*=1.4;player.stats.maxHp=Math.round(player.stats.maxHp*.75);player.hp=Math.min(player.hp,player.stats.maxHp)}return true}if(kind!=='item'||!DATA.items[id])return false;const level=player.items[id]||0;if(level>=DATA.items[id].max||!level&&Object.keys(player.items).length>=3)return false;player.items[id]=level+1;switch(id){case'speed':player.stats.speed*=1.1;break;case'vital':player.stats.maxHp+=20;player.hp+=20;break;case'rapid':player.stats.fireRate/=1.14;break;case'power':player.stats.damage*=1.2;break;case'multi':player.stats.projectiles++;break;case'cooldown':player.cooldownScale*=.9;break;case'lens':player.stats.projectileSize*=1.12;break;case'scope':player.stats.projectileLife+=.22;break;case'crystal':player.stats.critChance+=.035;player.stats.critDamage+=.12;break;case'lifesteal':player.stats.lifesteal+=.8;break;case'spikes':player.stats.thorns+=8;break;case'adrenaline':player.stats.adrenaline=(player.stats.adrenaline||0)+.1;break;case'bottle':player.stats.regen=(player.stats.regen||0)+.15;break;case'blade':player.stats.pierce++;break}return true}
class GameServer{
 constructor(now=()=>Date.now()){this.now=now;this.lobbies=new Map();this.clients=new Map()}
 addClient(send=()=>{}){const client={id:crypto.randomUUID(),send,lobby:'',connected:true,lastSeen:this.now(),rates:{},unlocked:['scout'],maps:['ruins'],disconnectAt:0};this.clients.set(client.id,client);return client}
 emit(client,type,data={}){try{client?.send({type,...data})}catch{}}
 broadcast(lobby,type,data={}){lobby.players.forEach(p=>this.emit(this.clients.get(p.id),type,data))}
 view(lobby){return{code:lobby.code,map:lobby.map,started:Boolean(lobby.game),players:lobby.players.map(({id,name,host,ready,character,connected})=>({id,name,host,ready,character,connected}))}}
 publish(lobby){lobby.updatedAt=this.now();this.broadcast(lobby,'lobbyState',{lobby:this.view(lobby)})}
 rate(client,type){const now=this.now(),last=client.rates[type];if(last!==undefined&&now-last<limits[type])return false;client.rates[type]=now;return true}
 validate(client,msg){if(!msg||typeof msg!=='object'||Array.isArray(msg)||!allowedMessages.has(msg.type)||!this.rate(client,msg.type))return false;if(msg.type==='playerInput')return ['up','down','left','right','q','e','r'].every(k=>msg[k]===undefined||typeof msg[k]==='boolean')&&finite(msg.aim,-Math.PI*4,Math.PI*4);if(msg.type==='joinLobby')return short(msg.code,6)&&/^[A-Z2-9]{6}$/.test(msg.code);if(msg.type==='selectMap')return['ruins','toxic'].includes(msg.map);if(msg.type==='selectCharacter')return['scout','warrior','druid'].includes(msg.character);if(msg.type==='upgradeChoice')return short(msg.choice,50);if(msg.type==='hello')return short(msg.clientId,80)&&Array.isArray(msg.unlocked)&&msg.unlocked.length<=3;return true}
 code(){let c;do{c=Array.from({length:6},()=>alphabet[Math.floor(Math.random()*alphabet.length)]).join('')}while(this.lobbies.has(c));return c}
 handle(client,msg){if(!this.validate(client,msg))return this.emit(client,'error',{message:'Nieprawidłowa lub zbyt częsta wiadomość.'});client.lastSeen=this.now();let lobby=this.lobbies.get(client.lobby),member=lobby?.players.find(p=>p.id===client.id);
  if(msg.type==='hello'){const requested=msg.clientId,old=this.clients.get(requested);if(old&&old!==client&&old.connected)return this.emit(client,'error',{message:'Ta sesja jest już połączona.'});this.clients.delete(client.id);client.id=requested;client.unlocked=['scout',...msg.unlocked.filter(x=>['warrior','druid'].includes(x))];client.maps=['ruins',...(msg.maps?.includes('toxic')?['toxic']:[])];client.connected=true;this.clients.set(client.id,client);for(const found of this.lobbies.values()){const p=found.players.find(x=>x.id===client.id);if(p&&this.now()-p.disconnectAt<=GRACE_MS){p.connected=true;p.disconnectAt=0;client.lobby=found.code;if(found.game?.players[p.id])found.game.players[p.id].input={};this.emit(client,'lobbyState',{lobby:this.view(found)});if(found.game)this.emit(client,'gameStarted',{lobby:this.view(found)});break}}return}
  if(msg.type==='createLobby'){this.leave(client,true);const code=this.code(),created={code,map:'ruins',createdAt:this.now(),updatedAt:this.now(),players:[{id:client.id,name:'Gospodarz',host:true,ready:true,character:'scout',connected:true,choiceRound:0}],game:null};this.lobbies.set(code,created);client.lobby=code;return this.publish(created)}
  if(msg.type==='joinLobby'){const target=this.lobbies.get(msg.code);if(!target||target.game||target.players.length>=MAX_PLAYERS)return this.emit(client,'joinError',{message:target?.players.length>=MAX_PLAYERS?'Lobby jest pełne.':'Nie znaleziono dostępnego lobby.'});this.leave(client,true);target.players.push({id:client.id,name:`Gracz ${target.players.length+1}`,host:false,ready:false,character:'scout',connected:true,choiceRound:0});client.lobby=target.code;return this.publish(target)}
  if(!lobby||!member)return this.emit(client,'error',{message:'Nie należysz do lobby.'});
  if(msg.type==='selectMap'&&member.host&&!lobby.game&&client.maps.includes(msg.map)){lobby.map=msg.map;lobby.players.forEach(p=>{if(!p.host)p.ready=false});return this.publish(lobby)}
  if(msg.type==='selectCharacter'&&!lobby.game&&client.unlocked.includes(msg.character)){member.character=msg.character;if(!member.host)member.ready=false;return this.publish(lobby)}
  if(msg.type==='playerReady'&&!member.host&&!lobby.game){member.ready=Boolean(msg.ready);return this.publish(lobby)}
  if(msg.type==='startGame'&&member.host&&!lobby.game&&lobby.players.filter(p=>!p.host).every(p=>p.ready)){this.start(lobby);return this.broadcast(lobby,'gameStarted',{lobby:this.view(lobby)})}
  if(msg.type==='playerInput'&&lobby.game&&!lobby.game.ended&&lobby.game.players[client.id])return lobby.game.players[client.id].input={up:!!msg.up,down:!!msg.down,left:!!msg.left,right:!!msg.right,q:!!msg.q,e:!!msg.e,r:!!msg.r,aim:msg.aim};
  if(msg.type==='upgradeChoice'&&lobby.game?.paused){const p=lobby.game.players[client.id];if(!p||member.choiceRound===lobby.game.levelRound||!p.offer.includes(msg.choice)||!applyUpgrade(p,msg.choice))return this.emit(client,'error',{message:'Ten upgrade nie jest dostępny.'});member.choiceRound=lobby.game.levelRound;p.offer=[];if(lobby.players.filter(x=>x.connected).every(x=>x.choiceRound===lobby.game.levelRound))lobby.game.paused=false;return}
  if(msg.type==='returnToLobby'&&lobby.game?.ended){lobby.game=null;lobby.players.forEach(p=>p.ready=p.host);return this.publish(lobby)}
 }
 start(lobby){const players={};lobby.players.forEach((m,i)=>{const stats=newStats(m.character);players[m.id]={id:m.id,name:m.name,character:m.character,x:1500+Math.cos(i*Math.PI/2)*55,y:1100+Math.sin(i*Math.PI/2)*55,hp:stats.maxHp,alive:true,kills:0,input:{},shot:0,items:{},augments:[],stats,cooldownScale:1,offer:[]}});lobby.game={createdAt:this.now(),tick:0,time:0,level:1,xp:0,nextXp:24,paused:false,ended:false,levelRound:0,players,enemies:[],bullets:[],orbs:[],loot:[],crates:[{id:'c1',x:1080,y:790,hp:28,opened:false},{id:'c2',x:1730,y:560,hp:28,opened:false}],spawn:0,bossAt:300}}
 offer(player){const candidates=Object.keys(DATA.items).filter(id=>(player.items[id]||0)<DATA.items[id].max&&((player.items[id]||0)>0||Object.keys(player.items).length<3)).map(id=>`item-${id}`);const aug=DATA.augments.filter(id=>!player.augments.includes(id)&&player.augments.length<3).map(id=>`augment-${id}`);return[...candidates,...aug].sort(()=>Math.random()-.5).slice(0,3)}
 disconnect(client){client.connected=false;client.disconnectAt=this.now();const lobby=this.lobbies.get(client.lobby),m=lobby?.players.find(p=>p.id===client.id);if(m){m.connected=false;m.disconnectAt=this.now();if(lobby.game?.players[m.id])lobby.game.players[m.id].input={};this.publish(lobby)}}
 leave(client,immediate=false){const lobby=this.lobbies.get(client.lobby);if(!lobby)return;const m=lobby.players.find(p=>p.id===client.id);if(immediate||!lobby.game)lobby.players=lobby.players.filter(p=>p.id!==client.id);else if(m){m.connected=false;m.disconnectAt=this.now()}client.lobby='';this.migrate(lobby);if(!lobby.players.length)this.lobbies.delete(lobby.code);else this.publish(lobby)}
 migrate(lobby){if(!lobby.players.some(p=>p.host&&p.connected)){lobby.players.forEach(p=>p.host=false);const next=lobby.players.find(p=>p.connected);if(next){next.host=true;next.ready=true}}}
 cleanup(){const now=this.now();for(const lobby of this.lobbies.values()){for(const m of [...lobby.players])if(!m.connected&&now-m.disconnectAt>GRACE_MS){lobby.players=lobby.players.filter(p=>p!==m);if(lobby.game)delete lobby.game.players[m.id]}this.migrate(lobby);const active=lobby.players.some(p=>p.connected),expired=!active&&now-lobby.updatedAt>GRACE_MS||!lobby.game&&now-lobby.updatedAt>LOBBY_TTL||lobby.game&&now-lobby.game.createdAt>GAME_TTL;if(!lobby.players.length||expired)this.lobbies.delete(lobby.code)}for(const[id,client]of this.clients)if(!client.connected&&now-client.disconnectAt>GRACE_MS)this.clients.delete(id)}
 tick(lobby,dt){const g=lobby.game;if(!g||g.paused||g.ended)return;g.tick++;g.time+=dt;g.spawn-=dt;if(g.spawn<=0){this.spawnEnemy(lobby);g.spawn=Math.max(.35,1.45-g.time/300)}for(const p of Object.values(g.players))this.tickPlayer(lobby,p,dt);for(const e of g.enemies)this.tickEnemy(lobby,e,dt);this.tickProjectiles(lobby,dt);const dead=g.enemies.filter(e=>e.hp<=0);dead.forEach(e=>{g.xp+=e.xp;g.orbs.push({id:crypto.randomUUID(),x:e.x,y:e.y,value:e.xp})});g.enemies=g.enemies.filter(e=>e.hp>0);if(g.time>=g.bossAt&&!g.enemies.some(e=>e.type==='boss')){this.spawnEnemy(lobby,'boss');g.bossAt+=300}if(g.xp>=g.nextXp){g.xp-=g.nextXp;g.level++;g.nextXp=Math.floor(g.nextXp*1.34);g.paused=true;g.levelRound++;lobby.players.forEach(m=>{m.choiceRound=0;const p=g.players[m.id];if(p)p.offer=this.offer(p)});lobby.players.forEach(m=>this.emit(this.clients.get(m.id),'levelUp',{round:g.levelRound,level:g.level,offer:g.players[m.id]?.offer||[]}))}if(Object.values(g.players).every(p=>!p.alive)){g.ended=true;this.broadcast(lobby,'gameOver',{reason:'allPlayersDead'})}this.broadcast(lobby,'gameState',{state:{tick:g.tick,time:g.time,level:g.level,xp:g.xp,nextXp:g.nextXp,paused:g.paused,ended:g.ended,players:Object.values(g.players).map(p=>({id:p.id,name:p.name,character:p.character,x:p.x,y:p.y,hp:p.hp,maxHp:p.stats.maxHp,alive:p.alive,kills:p.kills,cooldowns:p.stats.cooldowns})),enemies:g.enemies,bullets:g.bullets,orbs:g.orbs,loot:g.loot,crates:g.crates}})}
 spawnEnemy(lobby,type){const g=lobby.game,pool=['melee','melee','shooter','charger','brute','exploder','swarm'],kind=type||pool[Math.floor(Math.random()*pool.length)],d=DATA.enemies[kind],a=Math.random()*Math.PI*2;g.enemies.push({id:crypto.randomUUID(),type:kind,x:1500+Math.cos(a)*650,y:1100+Math.sin(a)*650,hp:d.hp,maxHp:d.hp,r:d.r,speed:d.speed,damage:d.damage,xp:d.xp,shot:1})}
 tickPlayer(lobby,p,dt){if(!p.alive)return;if(p.stats.regen)p.hp=Math.min(p.stats.maxHp,p.hp+p.stats.regen*dt);const i=p.input,dx=(i.right?1:0)-(i.left?1:0),dy=(i.down?1:0)-(i.up?1:0),len=Math.hypot(dx,dy)||1,nx=p.x+dx/len*p.stats.speed*dt,ny=p.y+dy/len*p.stats.speed*dt;if(!blocked(lobby.map,nx,p.y,15))p.x=nx;if(!blocked(lobby.map,p.x,ny,15))p.y=ny;p.shot-=dt;for(const k of['q','e','r'])p.stats.cooldowns[k]=Math.max(0,p.stats.cooldowns[k]-dt);if(p.shot<=0){for(let n=0;n<p.stats.projectiles;n++){const spread=(n-(p.stats.projectiles-1)/2)*.11,a=i.aim+spread;lobby.game.bullets.push({id:crypto.randomUUID(),owner:p.id,x:p.x,y:p.y,vx:Math.cos(a)*p.stats.projectileSpeed,vy:Math.sin(a)*p.stats.projectileSpeed,r:p.stats.projectileSize,life:p.stats.projectileLife,damage:p.stats.damage*(1+(1-p.hp/p.stats.maxHp)*(p.stats.adrenaline||0))*(Math.random()<p.stats.critChance?p.stats.critDamage:1),pierce:p.stats.pierce})}p.shot=p.stats.fireRate}for(const[k,base,radius,damage]of[['q',5,160,30],['e',9,190,35],['r',28,450,90]])if(i[k]&&p.stats.cooldowns[k]<=0){p.stats.cooldowns[k]=base*p.cooldownScale;for(const e of lobby.game.enemies)if(Math.hypot(e.x-p.x,e.y-p.y)<radius)e.hp-=damage}}
 tickEnemy(lobby,e,dt){const players=Object.values(lobby.game.players).filter(p=>p.alive);let target=null,best=Infinity;for(const p of players){const d=Math.hypot(p.x-e.x,p.y-e.y);if(d<best){best=d;target=p}}if(!target)return;const a=Math.atan2(target.y-e.y,target.x-e.x),nx=e.x+Math.cos(a)*e.speed*dt,ny=e.y+Math.sin(a)*e.speed*dt;if(!blocked(lobby.map,nx,e.y,e.r))e.x=nx;if(!blocked(lobby.map,e.x,ny,e.r))e.y=ny;if(best<e.r+15){target.hp=Math.max(0,target.hp-e.damage*dt);if(target.stats.thorns)e.hp-=target.stats.thorns*dt;if(target.hp<=0)target.alive=false}}
 tickProjectiles(lobby,dt){const g=lobby.game;for(const b of g.bullets){b.x+=b.vx*dt;b.y+=b.vy*dt;b.life-=dt;if(blocked(lobby.map,b.x,b.y,b.r))b.life=0;for(const e of g.enemies)if(b.life>0&&e.hp>0&&Math.hypot(b.x-e.x,b.y-e.y)<e.r+b.r){e.hp-=b.damage;b.pierce--;if(b.pierce<0)b.life=0;const owner=g.players[b.owner];if(e.hp<=0&&owner&&!e.killedBy){e.killedBy=owner.id;owner.kills++;if(owner.stats.lifesteal)owner.hp=Math.min(owner.stats.maxHp,owner.hp+owner.stats.lifesteal)}}}g.bullets=g.bullets.filter(b=>b.life>0)}
}
function createHttpServer(core=new GameServer()){const publicFiles=new Set(['index.html','style.css','game.js','enemy-data.js','map-data.js','enemy-visuals.js','feedback.js','achievements.js','upgrade-data.js','settings-data.js','shared-game-data.js','multiplayer.js','multiplayer-game.js']);const server=http.createServer((req,res)=>{const name=req.url==='/'?'index.html':decodeURIComponent(req.url.split('?')[0]).slice(1);if(!publicFiles.has(name)){res.writeHead(404);return res.end('Not found')}fs.readFile(path.join(__dirname,name),(error,data)=>{if(error){res.writeHead(404);return res.end('Not found')}res.writeHead(200,{'content-type':name.endsWith('.html')?'text/html; charset=utf-8':name.endsWith('.css')?'text/css; charset=utf-8':'text/javascript; charset=utf-8','cache-control':'no-cache'});res.end(data)})});const{WebSocketServer}=require('ws'),wss=new WebSocketServer({server,maxPayload:4096});wss.on('connection',ws=>{let alive=true;const client=core.addClient(payload=>{if(ws.readyState===1)ws.send(JSON.stringify(payload))});ws.on('pong',()=>alive=true);ws.on('message',(raw,isBinary)=>{if(isBinary||raw.length>4096)return ws.close(1008,'Invalid payload');try{core.handle(client,JSON.parse(raw.toString()))}catch{core.emit(client,'error',{message:'Nieprawidłowa wiadomość.'})}});ws.on('close',()=>core.disconnect(client));client.heartbeat=()=>{if(!alive)return ws.terminate();alive=false;ws.ping()}});const heartbeat=setInterval(()=>core.clients.forEach(c=>c.heartbeat?.()),10000),gameLoop=setInterval(()=>core.lobbies.forEach(l=>core.tick(l,1/TICK_RATE)),1000/TICK_RATE),cleanup=setInterval(()=>core.cleanup(),5000);for(const t of[heartbeat,gameLoop,cleanup])t.unref();server.on('close',()=>{clearInterval(heartbeat);clearInterval(gameLoop);clearInterval(cleanup);wss.close()});return{server,wss,core}}
if(require.main===module){const{server}=createHttpServer();server.listen(PORT,()=>console.log(`Swarmfall server: http://localhost:${PORT}`))}
module.exports={GameServer,createHttpServer,applyUpgrade,blocked,DATA};
