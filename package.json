{
  "name": "swarmfall",
  "version": "1.0.0",
  "private": true,
  "description": "Swarmfall with an authoritative WebSocket multiplayer server",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js",
    "syntax:check": "node --check account-store.js && node --check server.js && node --check account-client.js && node --check game.js && node --check progress-outbox.js",
    "lint": "eslint account-store.js server.js progress-outbox.js",
    "format": "prettier --write account-store.js server.js progress-outbox.js test/progression-hardening.test.js test/progress-outbox.test.js README.md",
    "format:check": "prettier --check account-store.js server.js progress-outbox.js test/progression-hardening.test.js test/progress-outbox.test.js README.md"
  },
  "dependencies": {
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "eslint": "^10.2.1",
    "prettier": "^3.8.3"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
